import { AgentTabView } from '@/components/agents/agent-tab-view';

export default function AuctionsPage() {
  return <AgentTabView agentType="auctions" agent={undefined} opportunities={[]} runs={[]} />;
}
