import { AgentTabView } from '@/components/agents/agent-tab-view';

export default function ListingsPage() {
  return <AgentTabView agentType="listings" agent={undefined} opportunities={[]} runs={[]} />;
}
