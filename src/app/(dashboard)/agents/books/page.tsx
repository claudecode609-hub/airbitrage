import { AgentTabView } from '@/components/agents/agent-tab-view';

export default function BooksPage() {
  return <AgentTabView agentType="books" agent={undefined} opportunities={[]} runs={[]} />;
}
