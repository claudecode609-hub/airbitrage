import { AgentTabView } from '@/components/agents/agent-tab-view';

export default function RetailPage() {
  return <AgentTabView agentType="retail" agent={undefined} opportunities={[]} runs={[]} />;
}
