import { AgentTabView } from '@/components/agents/agent-tab-view';

export default function CollectiblesPage() {
  return <AgentTabView agentType="collectibles" agent={undefined} opportunities={[]} runs={[]} />;
}
